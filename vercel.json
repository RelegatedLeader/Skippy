{
  "buildCommand": "cp cloud/schema.cloud.prisma prisma/schema.prisma && npx prisma generate && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "BUILD_STANDALONE": "0"
  }
}
